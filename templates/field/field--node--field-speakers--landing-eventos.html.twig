{#
    /**
     * @file
     * Template for a field.
     */
    #} 
<section class="splideSpeakers">
    <ul>
        {% for item in items %}
        <li class="splide__slide">{{item.content }}</li>
        {% endfor %}
    </ul>
</section>