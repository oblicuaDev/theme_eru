{#
/**
 * @file
 * Template for a field.
 */
#} 
 
    <h3{{ title_attributes }}>{{ label }}</h3>
    {% if multiple %}
    <div>
      {% endif %}      
      {% for item in items %}
        <div class="btn link-proyecto">
        <i class="link-ico"></i>{{ item.content }}</div>      
      {% endfor %}
      {% if multiple %}
    </div>
    {% endif %}  