<div class="cust-fields">
	<span>{{ fields.title }},</span>
	<span>{{ fields.field_cover_img }}</span>
	<span>{{ fields.title.content }},</span>
	<span>{{ fields.body.content }}</span>
	<span>{{ dump(fields) }}</span>
	{{ row.content['#row']._entity.field_link[0]['#url'] }}

	<a href="{{ row.content['#row']._entity.field_link[0]['#url'] }}">
		{{ row.content['#row']._entity.field_link[0]['#title'] }}
	</a>
	{{ row.content['#row']._entity.field_cover_img }}
</div>
